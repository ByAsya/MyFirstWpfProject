<Window x:Class="MyProject.Sign"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyProject"
        mc:Ignorable="d"
        Height="250" Width="400"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        BorderThickness="1"
        BorderBrush="Gray"
        Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Rectangle Fill="#dcc6b3"/>
        <StackPanel Orientation="Horizontal">
            <Button Name="In" Width="200" Height="40" Background="Transparent" BorderBrush="Transparent" Content="Вход" FontFamily="Verdana" FontSize="18" Foreground="White" Click="Input_Click"/>
            <Button Name="Up" Width="200" Height="40" Background="Transparent" BorderBrush="Transparent" Content="Регистрация" FontFamily="Verdana" FontSize="18" Foreground="White" Click="Reg_Click"/>
        </StackPanel>

        <Grid Name="InUp" Grid.Row="1"/>
    </Grid>
</Window>
